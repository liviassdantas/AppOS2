<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".TelaPrincipal">

    <include
            android:id="@+id/layout_cadastra_os"
            layout="@layout/fragment_cadastar_os"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    <include
            layout="@layout/fragment_cadastrar_cliente"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="372dp"
            app:layout_constraintBottom_toBottomOf="@id/layout_cadastra_os"
            app:layout_constraintStart_toStartOf="@id/layout_cadastra_os" />
</androidx.constraintlayout.widget.ConstraintLayout>